import React from 'react';
import Container from './styles';

const Header: React.FC = () => (
  <Container>
    <header>
      <h1>Header</h1>
    </header>
  </Container>
);

export default Header;
